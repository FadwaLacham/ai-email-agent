[project]
name = "ai-email-agent"
version = "1.0.0"
description = "AI Email Agent - Multi-Agent Gmail Management System"
requires-python = ">=3.10,<3.14"

[tool.fastapi]
entrypoint = "backend.main:app"